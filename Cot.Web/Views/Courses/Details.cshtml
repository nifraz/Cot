@model CourseViewModel

@{
    ViewData["Title"] = "Course Details";
}

<!--Header-->
<div class="p-1 pt-0 pb-2 container-fluid border-bottom" style="background: url('@Url.Content("~/images/circuit-board.svg")');">
    <!--Top Section-->
    <div class="m-0 p-0 g-2 row">
        <div class="gap-2 col-12 d-flex align-content-center">
            <!--Title-->
            <h3 class="m-0 p-0">View <strong>Course</strong> Details</h3>
            <!--Main Buttons-->
            <a class="ms-auto btn btn-secondary" title="Go back to List" asp-action="Index"><i class="bi bi-arrow-left-circle"></i>&nbsp;BACK</a>
        </div>
    </div>
</div>

    <div class="row">
        <div class="col-sm-2">
            @Html.DisplayNameFor(model => model.Course.AddedDate)
        </div>
        <div class="col-sm-10">
            @Html.DisplayFor(model => model.Course.AddedDate)
        </div>

        @*<table class="table table-sm table-bordered table-striped">
                <tr><th>Id:</th><td>@Model.Id</td></tr>
                <tr><th>Name:</th><td>@Model.Name</td></tr>
                <tr><th>Date of Birth:</th><td>@Model.DOB.ToShortDateString()</td></tr>
                <tr><th>Role:</th><td>@Model.Role</td></tr>
                <tr><th>House No:</th><td>@Model.HomeAddress?.HouseNumber</td></tr>
                <tr><th>Street:</th><td>@Model.HomeAddress?.Street</td></tr>
                <tr><th>City:</th><td>@Model.HomeAddress?.City</td></tr>
                <tr><th>Postal Code:</th><td>@Model.HomeAddress?.PostalCode</td></tr>
                <tr><th>Country:</th><td>@Model.HomeAddress?.Country</td></tr>
            </table>*@
    </div>
</div>
<div>
    <a asp-action="Edit" asp-route-id="@Model.Course.Id">Edit</a> |
    <a asp-action="Index">Back to List</a>
</div>
